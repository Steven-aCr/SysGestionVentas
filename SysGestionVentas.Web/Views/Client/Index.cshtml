@model IEnumerable<SysGestionVentas.EN.Client>

@{
    ViewData["Title"] = "Clients";
}

<h1>Clients</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New Client</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Client Id</th>
            <th>Name</th>
            <th>Status ID</th>
            <th>Document Type ID</th>
            <th>Address</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var client in Model)
        {
            <tr>
                <td>@client.ClientId</td>
                <td>@client.Name</td>
                <td>@client.StatusId</td>
                <td>@client.DocumentTypeId</td>
                <td>@client.Address</td>
                <td>
                    <a asp-action="Details" asp-route-id="@client.ClientId">Details</a> |
                    <a asp-action="Edit" asp-route-id="@client.ClientId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@client.ClientId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
